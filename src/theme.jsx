import { createMuiTheme } from 'material-ui/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#ff0000',
      main: '#ff0000',
      dark: '#ff0000',
      contrastText: '#ff0000',
    },
    secondary: {
      light: '#ff0000',
      main: '#ff0000',
      dark: '#ff0000',
      contrastText: '#ff0000',
    },
  },
});

export default theme;
